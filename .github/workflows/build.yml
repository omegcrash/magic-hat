# Magic Hat — GitHub Actions CI
# Copyright (c) 2026 George Scott Foley — MIT License
#
# Builds ISO on tag push, publishes to GitHub Releases.

name: Build Magic Hat ISO

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number'
        required: true
        default: '0.1.0'

jobs:
  build-iso:
    runs-on: ubuntu-latest
    container:
      image: fedora:41
      options: --privileged

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build tools
        run: |
          dnf install -y lorax mkksiso curl coreutils

      - name: Determine version
        id: version
        run: |
          if [[ "${{ github.event_name }}" == "push" ]]; then
            echo "version=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT
          else
            echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          fi

      - name: Build ISO
        run: |
          chmod +x scripts/build-iso.sh
          ./scripts/build-iso.sh ${{ steps.version.outputs.version }}

      - name: Upload ISO artifact
        uses: actions/upload-artifact@v4
        with:
          name: magichat-iso
          path: dist/magichat-*.iso*

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/magichat-*.iso
            dist/magichat-*.sha256
          body: |
            ## Magic Hat ${{ steps.version.outputs.version }}

            Purpose-built Linux server for the Familiar AI ecosystem.

            ### Install
            1. Download the ISO
            2. Verify: `sha256sum -c magichat-*.sha256`
            3. Boot from ISO (bare metal or VM)
            4. Follow the first-boot wizard

            Or install on existing Fedora Server:
            ```bash
            curl -fsSL https://raw.githubusercontent.com/omegcrash/magic-hat/master/scripts/install.sh | sudo bash
            ```

  validate-kickstart:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate Kickstart syntax
        run: |
          # Basic syntax check — ensure no broken heredocs or unclosed sections
          grep -c '%packages' kickstart/magichat.ks | grep -q 1
          grep -c '%end' kickstart/magichat.ks | grep -q 2
          grep -c '%post' kickstart/magichat.ks | grep -q 1
          echo "Kickstart syntax OK"

      - name: Validate systemd units
        run: |
          for f in systemd/*.service systemd/*.timer; do
            if head -1 "$f" | grep -q '^\[Unit\]\|^#'; then
              echo "OK: $f"
            else
              echo "FAIL: $f — missing [Unit] header"
              exit 1
            fi
          done

      - name: Shellcheck scripts
        uses: ludeeus/action-shellcheck@master
        with:
          scandir: './scripts'
          severity: warning

      - name: Shellcheck security scripts
        uses: ludeeus/action-shellcheck@master
        with:
          scandir: './security'
          severity: warning

      - name: Validate security configs
        run: |
          # SSH config — verify critical settings
          grep -q "PermitRootLogin no" security/sshd_magichat.conf
          grep -q "PasswordAuthentication no" security/sshd_magichat.conf
          echo "SSH hardening: OK"

          # Kernel sysctl — verify ASLR
          grep -q "kernel.randomize_va_space = 2" security/sysctl-hardening.conf
          echo "Kernel hardening: OK"

          # Audit rules — verify immutable flag
          grep -q "^-e 2" security/audit.rules
          echo "Audit rules: OK"

          # seccomp — verify JSON validity
          python3 -c "import json; json.load(open('security/seccomp-reflection.json'))"
          echo "seccomp profile: OK"

          # Output filter — verify sections exist
          grep -q "\[patterns\]" security/output-filter.conf
          grep -q "\[allowlist\]" security/output-filter.conf
          echo "Output filter: OK"

          # fail2ban filters — verify definitions
          for f in firewall/filter.d/*.conf; do
            grep -q "failregex" "$f"
            echo "fail2ban filter $(basename $f): OK"
          done
