# Magic Hat — fail2ban filter for blocked exploit paths
# Matches requests to wp-admin, phpmyadmin, .env, .git, etc.

[Definition]
failregex = ^<HOST> .* "(GET|POST|HEAD) /(wp-admin|wp-login|xmlrpc|phpmyadmin|adminer|\.env|\.git)
ignoreregex =
