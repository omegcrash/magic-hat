# Magic Hat — fail2ban filter for API abuse detection
# Catches rapid-fire requests to agent/chat endpoints (OWASP ASI02)

[Definition]
failregex = ^<HOST> .* "(GET|POST) /api/(chat|agent|tool|generate)
ignoreregex =
